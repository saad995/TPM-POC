// import validation from "Modules/Common/Constants/validation";

// const FieldValidator = (value = "", field = "", obj: any) => {
//     let msg = "";
//     if (value === "") {
//         return `${field} Cannot be empty`;
//     }

//     switch (field) {
//         case "Email":
//             if (validation.Email.regex.test(value)) {
//                 msg = ""
//             }
//             break;
//         case "Cell":
//             if (validation.Cell.regex.test(value)) {
//                 msg = "";
//             }
//             break;
//     }

//     return msg;
// };

// module.exports = FieldValidator;

export default {};
